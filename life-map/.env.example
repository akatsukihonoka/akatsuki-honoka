# AI Interpretation (optional) — see src/lib/ai/README or the phase report
# for what this powers. LIFE MAP's core features work fully without it;
# when unset, AI explanation cards fall back to deterministic template text.
#
# Server-side only. Never expose these under a NEXT_PUBLIC_ prefix.
OPENAI_API_KEY=
OPENAI_MODEL=
